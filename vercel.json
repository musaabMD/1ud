{
  "installCommand": "cd su && npm install",
  "buildCommand": "cd su && npm run build",
  "outputDirectory": "su/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
